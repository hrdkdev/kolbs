[project]
name = "kolbs-reader"
version = "0.1.0"
description = "Local Flask app for running a simple Kolb learning worksheet."
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
]

[tool.uv]
package = false
